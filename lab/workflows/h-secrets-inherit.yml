name: H - reusable workflow secret inheritance
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  inherited:
    uses: cirewind-fixtures/workflows/.github/workflows/reusable-secret.yml@v1
    secrets: inherit
