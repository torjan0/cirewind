name: Synthetic reusable workflow receiving inherited secrets
on:
  workflow_call:
jobs:
  inherited-secret:
    runs-on: ubuntu-24.04
    steps:
      - name: Invoke marker with inherited fake secret
        uses: cirewind-fixtures/harmless@v1
        env:
          CIREWIND_LAB_INHERITED_INPUT: ${{ secrets.CIREWIND_LAB_INHERITED }}
