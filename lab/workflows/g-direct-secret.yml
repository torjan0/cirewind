name: G - direct named secret mapping
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  direct-secret:
    runs-on: ubuntu-24.04
    steps:
      - name: Invoke marker with one fake step-scoped secret
        uses: cirewind-fixtures/harmless@v1
        env:
          CIREWIND_LAB_DIRECT_INPUT: ${{ secrets.CIREWIND_LAB_DIRECT }}
