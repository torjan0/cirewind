# Release-candidate build and verification process

Status: implemented experimental-v0.1 candidate and authenticated-release
tooling

The local process creates deterministic, hash-verifiable release candidates.
The manual GitHub workflow can add GitHub build-provenance attestations and,
after two separately protected environment gates, create and publish a release.
No release workflow has yet been exercised on GitHub, and a local checksum still
does not prove who built an artifact. Supported-host runtime qualification and
the project-wide release decision remain separate release gates.

## Build contract

An official candidate is built from a completely clean checkout at an existing
annotated `vSEMVER` tag. The tag must identify `HEAD`. The build derives:

- the version from the tag without its `v` prefix;
- the full Git object ID from `HEAD`;
- `SOURCE_DATE_EPOCH` and the displayed UTC build date from the commit time; and
- the exact Go patch version from `go.mod`.

After validating the worktree and tag, `scripts/release.sh` creates an immutable
`git archive` snapshot of the exact commit and builds from that snapshot. It
rejects submodules and symlinks rather than allowing source outside the archived
Git object to affect a target. A concurrent edit to the operator's checkout
therefore cannot mix source revisions across the six binaries.

The six reviewed candidate targets are:

- `linux/amd64` and `linux/arm64`;
- `darwin/amd64` and `darwin/arm64`; and
- `windows/amd64` and `windows/arm64`.

Every target uses `CGO_ENABLED=0`, `-trimpath`, `-buildvcs=false`, an empty Go
linker build ID, and the same validated version/commit/date linker metadata.
The packaging verifier checks an exact release stamp in every foreign-platform
binary without executing it, checks the embedded Go toolchain/target/build graph,
and rejects module replacements. Native runtime smoke testing independently
checks the displayed version metadata.

Run the complete source-controlled build only from a committed annotated tag:

```sh
make release \
  RELEASE_TAG=v0.1.0 \
  RELEASE_OUT=/new/path/cirewind-v0.1.0 \
  RELEASE_WORK_ROOT=/large/private/work/path
```

The output path must not exist. Intermediate files are created in a validated
private temporary directory and removed. The finished directory is renamed into
place only after complete verification.

## Candidate contents

Unix targets use deterministic `.tar.gz` files; Windows targets use deterministic
`.zip` files. Each archive has one top directory and contains:

- the platform executable;
- `README.md`, `LICENSE`, `SECURITY.md`, and `THIRD_PARTY_NOTICES.md`;
- the harmless synthetic demonstration incident pack;
- `build-metadata.json` with `authenticated: false`;
- `sbom.spdx.json`; and
- a target-filtered `licenses/index.json` plus every complete indexed license,
  notice, and patent file applicable to modules embedded in that exact binary.

Each SPDX 2.3 JSON document is generated from `debug/buildinfo` in the exact
binary, not from the broader `go.mod` graph. The external per-target SPDX file
must byte-match the copy in its archive and a document regenerated by the
verifier. Module `h1` content hashes are represented as SHA-256 package
checksums. License conclusions for dependencies remain `NOASSERTION`; reviewed
license files are distributed separately rather than turning engineering review
into a legal attestation.

The distribution root contains twelve target files (six archives and six SPDX
documents), `release-metadata.json`, and `SHA256SUMS`. `SHA256SUMS` covers every
material root file except itself. It cannot be self-referential.

## Reproducibility and local verification

The reproducibility contract is byte-for-byte equality for two builds from the
same source commit, exact Go patch toolchain, target matrix, module graph, and
source-date epoch. It is not a looser semantic comparison. `make release-test`
builds all six synthetic candidates twice, compares every byte, runs the native
offline command smoke, and proves that a one-byte SBOM modification is rejected:

```sh
make release-test RELEASE_WORK_ROOT=/large/private/work/path
```

The test creates a disposable deterministic Git repository, commit, and
annotated `v0.0.0-repro.test` tag from the current source set. That temporary
object ID is fixture provenance, never project-release provenance or a
publishable candidate. The test neither changes the project Git index nor
retains its candidates.

Verify an intact local candidate from source with:

```sh
make release-verify \
  RELEASE_OUT=/path/to/cirewind-v0.1.0 \
  RELEASE_WORK_ROOT=/large/private/work/path
```

This rejects an unexpected or missing root file, malformed checksum line,
digest/length mismatch, duplicate or unsafe archive entry, link or device,
unindexed or modified license, target/build-graph mismatch, replaced Go module,
release-stamp mismatch, or disagreement between embedded/external/regenerated
SPDX. It verifies integrity and internal consistency, not publisher identity.

An independent SPDX parser remains a separate release check. With an
operator-installed, reviewed version of the official SPDX tools-python CLI:

```sh
make release-spdx \
  RELEASE_OUT=/path/to/cirewind-v0.1.0 \
  SPDX_VALIDATOR=/reviewed/venv/bin/pyspdxtools \
  SPDX_TOOLS_VERSION=0.8.5
```

The wrapper resolves the selected executable, verifies the exact installed
`spdx-tools` distribution version through its adjacent Python interpreter,
disables user-site imports, requires exactly six external documents, and parses
and validates each as SPDX 2.3. It does not install or update packages. Release
automation must pin the validator and its transitive artifacts before making it
a network-installed CI dependency.

Users who have only downloaded artifacts can first authenticate the maintainer's
signature or platform attestation, then use a local checksum utility. For
example, on Linux:

```sh
sha256sum --check SHA256SUMS
```

On macOS use `shasum -a 256 -c SHA256SUMS`. On Windows use
`Get-FileHash -Algorithm SHA256` and compare each result to the authenticated
checksum list. The final README installation text must name the authentication
mechanism actually selected for v0.1 rather than promising one in advance.

## Runtime smoke boundaries

`scripts/smoke-release.sh` verifies the distribution, unsets all supported
GitHub-token environment variables, then runs the native executable through:

- `version` and root help;
- offline `investigate --help` argument wiring;
- `pack validate` on the packaged synthetic incident;
- synthetic archive import;
- offline replay and complete case generation; and
- case-manifest verification.

The investigation smoke is deliberately help-only because a functional
investigation requires controlled GitHub evidence; it is not represented as a
live collection test. The command-level collector behavior remains covered by
mock tests and must separately pass the controlled GitHub.com qualification.

`scripts/container-smoke-release.sh` can repeat the Linux amd64 smoke in an
already-present minimal image with no network, a read-only root filesystem, no
capabilities, `no-new-privileges`, an unprivileged UID, and an ephemeral tmpfs.
It requires `CIREWIND_SMOKE_IMAGE_ID=sha256:...`, verifies that exact 64-hex
local image ID, and never accepts a mutable tag or pulls an image automatically.
The image ID and host must be recorded with release validation evidence. A
container smoke does not replace native macOS, Windows, or arm64 runtime
qualification.

`scripts/smoke-release-wine.sh` runs the Windows amd64 archive through the same
offline command path under an operator-supplied Wine installation and prefix.
It keeps its extraction and case files under the explicit work root. This is a
useful Windows API and path-compatibility test, but Wine on Linux is not a native
Windows host and cannot qualify Windows installation, endpoint security, or
filesystem behavior. The 2026-08-21 Wine pass is recorded in
[`docs/validation/2026-08-21-release-authentication.md`](validation/2026-08-21-release-authentication.md).

## GitHub workflow and authority boundary

`.github/workflows/release-candidate.yml` has only a `workflow_dispatch`
trigger. The dispatch ref and the input must be the same existing annotated
`vSEMVER` tag, and that tag must resolve to checked-out `GITHUB_SHA`. This
requirement binds the GitHub OIDC source ref, source digest, signer workflow
digest, release source, and release tag to the same commit. A branch dispatch,
pull request, lightweight tag, absent tag, noncanonical version, dirty source,
or tag/HEAD disagreement fails before packaging.

The `build` job has only `contents: read`, `attestations: write`, and
`id-token: write`. It builds the six-target distribution twice, compares every
byte, runs the native Linux smoke, and uses the official `actions/attest` action
to create SLSA build-provenance attestations whose subjects are all fourteen
root files: six archives, six external SPDX documents, `release-metadata.json`,
and `SHA256SUMS`. It then uploads those same bytes once and exposes the immutable
workflow artifact ID. Checkout credentials are never persisted.

The `draft` job runs only after the `release-draft` environment gate. In the
current solo-maintainer repository, the gate requires the repository-owner user
as its sole reviewer but permits that user to approve a run they initiated.
It receives the exact artifact ID without rebuilding the release, deep-verifies the
distribution, and runs `gh attestation verify` on every subject. Verification
requires all of the following before any `gh release create` call:

- the repository identity;
- the exact `.github/workflows/release-candidate.yml` signer workflow;
- the exact tagged signer and source commit;
- the exact `refs/tags/vSEMVER` source ref;
- the SLSA provenance predicate; and
- a GitHub-hosted rather than self-hosted attesting runner.

The job creates a draft only after those checks, or accepts an existing draft
only if its downloaded asset set is byte-for-byte identical to the newly
reproduced and attested subjects. `contents: write` exists only in the draft and
publish jobs. `actions: read` permits a fail-closed check of the environment
configuration; `attestations: read` permits provenance verification. All other
permissions are `none`.

If the explicit boolean `publish` input is true, the `publish` job waits at the
separate `release-publish` environment. It downloads the original artifact by
ID, rechecks the tag and every attestation, downloads and byte-compares the draft
immediately before changing it, and finally runs `gh release edit --draft=false
--verify-tag`. It never rebuilds or substitutes a release asset. Running first
with `publish=false` leaves an inspectable draft; a later `publish=true` run can
reproduce and attest the same bytes, accept that exact draft, and enter the
publication gate.

### Repository settings required before the first run

The workflow file cannot configure its own protection rules without acquiring
administrative authority, so the following are maintainer-controlled release
gates:

1. Create `release-draft` and `release-publish` environments.
2. On each environment, select exactly the repository-owner user as the sole
   required reviewer. For this solo-maintainer repository that is `torjan0`.
   Leave **Prevent self-review** disabled: GitHub otherwise prohibits the person
   who initiated the workflow from approving it, so the only authorized reviewer
   could never release. Deselect **Allow administrators to bypass configured
   protection rules**.
3. Select **Selected branches and tags**, add exactly one **tag** rule named
   `v*`, and add no branch rule. The exact annotated canonical `vSEMVER` and
   checked-out-commit checks in the workflow remain narrower than this
   environment pattern. The checked-in verifier reads both environment REST
   endpoints and fails before a release write unless the expected sole user,
   `prevent_self_review=false`, `can_admins_bypass=false`, custom-policy mode,
   and the single `tag:v*` rule are all visible. It derives the expected reviewer
   from `GITHUB_REPOSITORY_OWNER`; transfer to an organization therefore fails
   closed until this solo-maintainer policy is deliberately replaced.
4. Enable immutable releases before publication if that repository feature is
   available. GitHub documents that draft releases remain mutable and
   immutability begins only after publication.
5. Confirm the repository plan supports both protection rules and artifact
   attestations. GitHub makes attestations and required reviewers available to
   public repositories on current plans; private/internal attestation use
   requires GitHub Enterprise Cloud, and required reviewers on Free/Pro/Team
   are public-repository-only.
6. Review `.github/actions-pins.json`. Each action reference is a full object ID
   retrieved from its official `actions/*` repository; a tag comment is context,
   never the security boundary.

Allowing the initiator to approve is a conscious solo-maintainer residual risk,
not independent review. The environment approval remains a deliberate pause and
an authorization record. Compensating controls are the exact annotated-tag and
commit binding, exact signer/source attestation checks, disabled administrator
bypass, tag-only environment rule, immutable artifact-ID transfer, and byte
comparison. Run `publish=false` first, inspect the created draft and its assets,
then use a separate deliberate `publish=true` dispatch and approval; do not
approve the publication gate before that inspection. Adding a second trusted
maintainer should cause this exception to be retired by enabling **Prevent
self-review** and naming an independent reviewer.

After the workflow exists at the release tag, a maintainer can request a draft
without embedding credentials in a command:

```sh
gh workflow run release-candidate.yml \
  --ref v0.1.0 \
  -f tag=v0.1.0 \
  -f publish=false
```

Use `publish=true` only when both protected gates are intended to run. The
workflow performs GitHub writes; local release commands and default tests do
not. No source-controlled step pushes or creates a tag.

GitHub's attestation documentation describes provenance as a signed claim about
where and how bytes were built, not proof that the bytes are secure. Until the
protected workflow and supported-host gates pass for the eventual tag,
acceptable terms remain “unsigned local release candidate” and “Wine
compatibility-tested.” Do not call the files a supported install or completed
v0.1 release.

Primary references, retrieved 2026-08-21:

- [Using artifact attestations to establish provenance for builds](https://docs.github.com/en/actions/how-tos/secure-your-work/use-artifact-attestations/use-artifact-attestations)
- [Artifact attestations](https://docs.github.com/en/actions/concepts/security/artifact-attestations)
- [Deployments and environments](https://docs.github.com/en/actions/reference/workflows-and-actions/deployments-and-environments)
- [Reviewing deployments](https://docs.github.com/en/actions/how-tos/deploy/configure-and-manage-deployments/review-deployments)
- [Deployment environments REST endpoints](https://docs.github.com/en/rest/deployments/environments?apiVersion=2026-03-10)
- [Deployment branch-policy REST endpoints](https://docs.github.com/en/rest/deployments/branch-policies?apiVersion=2026-03-10)
- [`gh attestation verify`](https://cli.github.com/manual/gh_attestation_verify)
- [`gh release create`](https://cli.github.com/manual/gh_release_create)
- [`gh release edit`](https://cli.github.com/manual/gh_release_edit)
