name: fixture explicit reusable secret map
on: workflow_dispatch
jobs:
  mapping:
    uses: cirewind-fixtures/workflows/.github/workflows/reusable-mapped.yml@cccccccccccccccccccccccccccccccccccccccc
    secrets:
      TARGET: ${{ secrets.CIREWIND_LAB_DIRECT }}
