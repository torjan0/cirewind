name: fixture reusable callee with inherited fake secret
on:
  workflow_call:
jobs:
  nested:
    runs-on: ubuntu-24.04
    steps:
      - uses: cirewind-fixtures/harmless@v1
        env:
          CIREWIND_LAB_INHERITED_INPUT: ${{ secrets.CIREWIND_LAB_INHERITED }}
