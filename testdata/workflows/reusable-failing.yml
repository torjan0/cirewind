name: fixture reusable callee with expected harmless failure
on:
  workflow_call:
jobs:
  retry-target:
    runs-on: ubuntu-24.04
    steps:
      - uses: cirewind-fixtures/harmless@v1
      - name: End with expected fixture failure
        run: "exit 1"
